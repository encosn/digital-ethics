# digital-ethics — 디지털 윤리 모둠 탐구

## ① 앱의 목적과 대상 단원

**V. 디지털 문화 — 2. 디지털 윤리** 를 **모둠별 주제 탐구·발표 수업**으로 진행하는 앱.
[비상교육] 중학교 정보 교사용 교과서 **221~237쪽** 네 주제를 그대로 옮겼다.

**한 모둠 = 한 주제.** 네 주제를 네 모둠(또는 여덟 모둠)이 나눠 맡아 각각 발표한다.
(사용자가 2026-08-12 에 「한 모둠 = 한 주제」로 확정했다. 「한 모둠이 4주제 전부」는 안 만들었다)

| 주제 | 교과서 | 소주제 |
|:---|:---|:---|
| 🚫 사이버 폭력 예방 | 221~226쪽 | ① 디지털 윤리의 필요성 ② 사이버 폭력 및 범죄의 유형 ③ 예방과 대처 |
| 📵 디지털 중독 예방 | 227~229쪽 | ① 디지털 중독이란 ② 유형 ③ 대처 방안 |
| 🔒 개인 정보 보호 | 230~233쪽 | ① 개인 정보의 의미 ② 개인 정보 침해 ③ 보호 방법 |
| ©️ 저작권 보호 | 234~237쪽 | ① 저작권의 의미와 필요성 ② 저작물을 이용하는 방법 |

**성취기준**: [9정05-02] (안전한 생활 규칙을 논의하고 실천 방안 수립),
[9정05-03] (개인 정보·권리·저작권 보호 실천 방법 탐구)

**포트 8089.**

## ② 기능 목록

화면은 **5단계**다. 위쪽 단계 막대로 오갈 수 있고, 주제를 고르기 전에는 2~5단계가 잠긴다.

### 1단계 — 주제 고르기
- 모둠 이름 입력 (브라우저에만 저장)
- 주제 카드 4장. 카드에 **소주제 목록과 교과서 쪽수**가 함께 나와 어떤 내용인지 보고 고를 수 있다.

### 2단계 — 교과서 정리 보기 ★ 이 앱의 핵심
교과서 내용을 **인포그래픽 블록**으로 정리한 화면. 소주제 단추로 옮겨 다닌다.
**다른 주제도 열어 볼 수 있다** — 다른 모둠 발표를 들을 때 참고하라고 만들었다.

주요 시각화:
- **사이버 폭력** — 디지털 공간 특징 4개 카드 / **실태 조사 막대그래프**(피해자가 다시 가해자: 청소년 43.9% vs 성인 31.6%, 가해자의 피해 경험 79.9% vs 71.5%) / **가해 동기 순위 좌우 비교**(청소년 1위 「보복」 38.4% ↔ 성인 1위 「재미·장난」 39.2%) / 폭력 유형 6장 / 대처 5단계 흐름 / 10대 수칙 / 피싱·스미싱·파밍·가짜뉴스
- **디지털 중독** — 중독 위험군 증감(인터넷 183,228→188,978명, 스마트폰 129,543→134,832명) / 폐해 6가지 / 유형 4가지와 교과서 사례 / **5WHY 흐름도**(예시 포함)
- **개인 정보** — 정보 4분류(인적·신체·행위·교육) / 침해 4단계 / SNS 사진 속 개인 정보 2건 / 피해→대처 5쌍 / 신고처
- **저작권** — 저작물 9종 / 보호받지 못하는 것 3가지 / 보호의 필요성 4가지 / **CC 라이선스 마크 4개** / **공공누리 4유형 표** / 알쏭달쏭 저작권 5문답

### 3단계 — 내용 정리하기
- **핵심 확인 퀴즈 6문항** (O×·선택형·빈칸형 섞음). 힌트 단추, 정답 확인, 해설, 다시 풀기.
- **우리 모둠의 정리 3칸** — ① 한 문장 요약 ② 왜 문제가 되나 ③ 실천 방법 3가지.
  발표 슬라이드에 그대로 들어간다.

### 4단계 — 실제 사례 조사하기
- **교과서에 나온 사례**를 보기로 먼저 보여 준다 (무엇을 어떻게 쓰는지 본보기).
- **조사할 때 찾아볼 곳** — 그 주제의 신고·상담 기관 링크.
- **사례 카드 최대 4장** — 제목 / 무슨 일 / 어떤 피해 / 예방·대처 / 출처(선택).
  칸을 채우는 즉시 「✔ 다 채웠어요 / 아직 N칸 남았어요」가 바뀐다.

### 5단계 — 발표하기
- **점검하기 5줄** — 주제·모둠 이름·퀴즈·정리·사례를 검사해 빠진 곳을 말로 알려 준다.
- **전체 화면 발표 모드** — ← → 또는 스페이스로 넘기고 Esc 로 끝낸다. 교실 TV 크기에 맞춰 글자가 커진다.
- **PDF로 저장** — 브라우저 인쇄 기능.
- **처음부터 다시 하기** — 다음 반 수업 전에 지울 때.

## ③ 현재 개발 상태와 다음 할 일

**1차 완성 (2026-08-12).** 시험 141개 통과 (`ethics-core` 42 + `topics` 99).
개발 서버·빌드·되돌아오기(새로고침 후 이어 하기)까지 브라우저로 확인했다.

**아직 안 한 것**
- [ ] **GitHub 저장소·Pages 배포** — 저장소도 워크플로도 아직 없다.
      배포하면 `hub/data.js` 5단원의 `status` 를 `'ready'` 로 바꾸고 `url` 을 채워야 한다.
- [ ] **`index.html` 더블클릭 실물 확인** — 외부 의존성 0개, ES 모듈·fetch·CDN 미사용이라
      열려야 하지만, 개발 환경의 브라우저가 `file://` 를 막아 확인하지 못했다.
      (`ai-explorer`·`algorithm-flowchart`·`data-report` 도 같은 상태다)
- [ ] 실제 수업에서 써 보고 4단계 사례 칸의 개수·분량이 한 차시에 맞는지 확인

## 파일 구성

| 파일 | 하는 일 |
|:---|:---|
| `index.html` | 5단계 판(panel)의 뼈대. 내용은 모두 `main.js` 가 채운다 |
| `style.css` | 꾸미기. 본문 18px, 제목 28~34px (교실 TV 기준). 인쇄용 `@media print` 포함 |
| `topics.js` | **교과서 내용 전부.** 4주제 × 소주제 × 시각화 블록 + 퀴즈 + 예시 사례 |
| `ethics-core.js` | 채점·점검·슬라이드 구성·저장 직렬화. **DOM 을 건드리지 않는다** |
| `main.js` | 화면 그리기와 눌렀을 때의 동작 |
| `tests/run-core-tests.js` | `ethics-core` 시험 42개 |
| `tests/run-topic-tests.js` | `topics.js` 자료 정합성 시험 99개 |

**교과서 내용만 고치고 싶으면 `topics.js` 하나만 보면 된다.** 화면 코드를 건드릴 필요가 없다.

## 왜 이렇게 만들었는지 (고치기 전에 읽을 것)

### 학번·이름을 저장하지 않는다 ★
5단계에 학번·모둠원 이름 칸이 있지만 **PDF 머리말에만 쓰고 저장하지 않는다.**
교실 공용 컴퓨터에서 다음 학생에게 앞 학생 이름이 남으면 안 되기 때문이다.
(`algorithm-flowchart` 와 같은 규칙)

구현: 이름은 `state.student` 에 담기고, 저장은 **반드시 `ETHICS.toSaved()` 를 거치는데
그 함수가 `student` 를 통째로 뺀다.** 단위 시험이 이것을 지킨다
(`★ 저장할 때 학번·이름이 빠진다`). **저장 코드를 고칠 때 이 시험을 지우지 말 것.**

### 「중독 위험군」 그래프에 학년별 숫자를 안 붙인 이유 ★
교과서 227쪽 [그림 V-3] 은 초4·중1·고1 **세 학년을 색으로 구분한** 꺾은선그래프다.
숫자 여섯 쌍은 PDF 에서 뽑아냈지만 **어느 색이 어느 학년인지는 텍스트로 알 수 없었고**
(이 환경에는 PDF 를 그림으로 바꾸는 도구가 없다), 추측해서 붙이면 학생에게 틀린 사실을
가르치게 된다. 그래서 **세 학년을 합한 값과 「세 학년 모두 늘었다」만** 보여 준다.
합계가 교과서 숫자와 맞는 것은 확인했다(증가분 1,522+175+4,053=5,750 / 1,953+1,634+1,702=5,289).
→ **나중에 원본 그림을 볼 수 있게 되면** 학년별로 나눠 그리는 것이 더 좋다.

### 가해 동기 수치는 열 위치를 확인해서 확정했다
221쪽의 두 순위 그래프는 `pdftotext -layout` 으로 뽑으면 청소년/성인 숫자가 뒤섞인다.
`pdftotext -table` 로 다시 뽑아 **열 위치를 보고** 짝을 맞췄다.
시험(`가해 동기 1위 — 청소년은 보복, 성인은 재미·장난`)이 이 값을 지킨다.

### 디지털 중독 주제의 상담 기관 3곳은 교과서에 없다
교과서 229쪽에는 신고·상담처가 없다. 그런데 4단계 「조사할 때 찾아볼 곳」이 비면
학생이 어디서 사례를 찾을지 막막해진다. 그래서 **스마트쉼센터(1599-0075) ·
청소년사이버상담센터(1388) · 한국도박문제예방치유원(1336)** 을 넣었다.
나머지 세 주제의 기관은 모두 교과서에 나온 것이다.

### 사례를 자동으로 검색해 주지 않는다
「실제 사례를 조사한다」가 이 수업의 활동 목표다. 앱이 사례를 찾아 주면 활동이 사라진다.
그래서 **교과서 예시 한 건을 본보기로 보여 주고, 기관 링크만 준다.**
학생은 직접 검색해 다섯 칸을 채운다.

### PDF 는 외부 라이브러리 없이 브라우저 인쇄로 만든다
jsPDF 같은 라이브러리는 한글 글꼴을 따로 넣어야 하고 안 넣으면 글자가 깨진다.
`window.print()` + `@media print` 로 하면 **한글이 안 깨지고** 글자가 벡터로 들어간다.
(`algorithm-flowchart` 와 같은 방식)

### 채점은 띄어쓰기·문장부호를 무시한다
`ETHICS.normalize()` 가 공백과 흔한 문장부호를 지우고 견준다.
「개인 정보 침해」와 「개인정보침해」를 둘 다 정답으로 인정하기 위한 것이다.
빈칸형 문항을 새로 넣을 때 **답이 한 낱말이 되게** 만들 것 — 문장을 답으로 두면
정규화로도 못 맞힌다.

### 사례 칸에 글을 쓸 때는 다시 그리지 않는다
`renderCases()` 를 매번 부르면 글자를 치던 칸에서 커서가 빠진다.
그래서 입력 중에는 **완성 표시(뱃지·테두리·개수)만 손으로 바꾼다** (`main.js` 의 `cases` input 처리).
사례를 추가·삭제할 때만 다시 그린다.

## 개발 명령 (이 폴더 안에서)

| 명령 | 하는 일 |
|:---|:---|
| `npm install` | 의존성 설치 (vite 하나뿐) |
| `npm start` | 개발 서버 → http://localhost:8089 |
| `npm test` | 단위 시험 141개 |
| `npm run build` | `dist/` 생성 |
| `npm run preview` | 빌드 결과 확인 (개발 서버를 먼저 끌 것) |

루트에서 띄우려면 `npm --prefix ./digital-ethics start`.
(`C:\project_AI\.claude\launch.json` 에 `digital-ethics` 로 등록해 두었다)

## 주의

- **스크립트 파일을 추가하면 `vite.config.mjs` 의 `copyPlainScripts([...])` 목록에도 넣어야 한다.**
  안 넣으면 개발 서버에서는 되는데 `dist/` 에 파일이 빠져 빌드본이 안 열린다.
- `topics.js` 에 블록을 새로 넣을 때는 **`main.js` 의 `renderBlock` 이 아는 `kind`** 여야 한다.
  모르는 kind 는 조용히 빈칸이 되는데, `tests/run-topic-tests.js` 가 이것을 잡아 준다.
- 이 앱은 학생 개인정보를 **수집하지도 저장하지도 않는다.** 로그인·서버 전송도 없다.
